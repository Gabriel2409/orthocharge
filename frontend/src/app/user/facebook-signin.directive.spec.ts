import { FacebookSigninDirective } from './facebook-signin.directive';

describe('FacebookSigninDirective', () => {
  it('should create an instance', () => {
    const directive = new FacebookSigninDirective();
    expect(directive).toBeTruthy();
  });
});
